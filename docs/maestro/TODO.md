# Maestro Todo

The audited migration-source checkpoint implementation and focused tests cover lifecycle, persistence, IPC authentication, attach/detach, turn leases, workspace safety, service supervision, diagnostics, and the dashboard. Reconfirm these boundaries after transfer.

Still requiring current artifact evidence:

- Windows and Linux install/start/stop/restart/uninstall certification;
- upgrade and rollback from the previous release;
- crash-loop and forced-recovery certification;
- installed TUI attach/search verification;
- canonical state consistency across lifecycle, supervisor, dashboard, and health;
- exact release-manifest and artifact checks.

Do not reopen completed O0–O8 planning phases from old docs. Add new work here only after checking current source/tests and recording a concrete gap.
