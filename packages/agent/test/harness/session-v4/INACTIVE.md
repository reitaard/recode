# Deferred V4 AgentHarness tests

These upstream v0.84.1 tests require the V4 AgentHarness application lifecycle and its `harness/session/**` runtime surface. Recode retains Session V4 as an isolated library while Coding Agent remains on the upstream-compatible V3 SessionManager runtime. The `.inactive` files are preserved reference tests; activate them only with the separately approved V4 runtime migration.
